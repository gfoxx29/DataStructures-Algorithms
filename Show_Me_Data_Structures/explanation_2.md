We have written a recursive fonction which walks a directory tree and gathers files that contain
a given suffix:

	- Time complexity is O(n) with n number of directory in each level
	- Space complexity O(n)